# HANDOFF — arrancar sesión limpia (25 jun 2026)

> Punto de entrada para una sesión nueva. Supersede los bloques de `NEXT-SESSION.md` en lo que toca
> al ladder. El report (lo que paga) sigue gobernado por `STRATEGY-PLAN.md`.

## Estado en una frase
Mega **v2 convergió 827.7 (+187** vs el Mega sin parchear 640.7) → bucle código→ladder→Elo VALIDADO.
**v3** (Mega + consistency-bands) subido (id `54034884`), par activo en el ladder = **{v3, v2 827.7}**, en
convergencia (μ−3σ, días). **4 levers KB** del conocimiento descargado (kb_draw/kb_role/kb_seq/kb_prizemap)
construidos + A/B N=200 limpios pero **dentro del ruido** y **SIN ladder**. Módulo de **disciplina de ladder**
(deflación family-wise) construido y verificado. Report endurecido (Spearman + agreement a `verified`).

## Cómo arrancar (orden de lectura)
1. Este fichero → `STRATEGY-PLAN.md` (el report = lo que paga, $240K/8×$30K) → `research/ab_logs/RESULTS-N200-2026-06-24.md` → `research/ladder_discipline/README.md`.
2. Memoria viva: `memoria-claude/proyectos/ptcg-pivote-sabrina-alakazam.md` + `ptcg-ladder-discipline-quant-import.md`.
3. Si vas a tocar el engine: `colima start` + Docker `ptcg-cabt` (ver NEXT-SESSION "Cómo levantar el entorno").
4. Estado del ladder: `.venv/bin/kaggle competitions submissions pokemon-tcg-ai-battle`.

## Trabajo AUTÓNOMO (no necesita a Fran — submit funciona headless vía `~/.kaggle/access_token`)
- **Cuando v3 converja:** leer su Elo vs v2 (827.7). Registrar el experimento en `research/ladder_discipline/ladder_ledger.py`. Decidir por Elo, **NUNCA por cabt** (anti-predictivo, Spearman −0.80).
- **Rotar cola KB, 1 por ventana de convergencia, manteniendo un suelo:** `kb_draw` (mejor mecanismo, anti-deck-out) → `kb_role` → `kb_prizemap`/`kb_seq`. **Pre-registrar la familia** (`PREREGISTER.md`) ANTES de subir. Con ≥2 levers convergidos, computar `grid_dispersion(gaps)` y pasar el **gate deflactado** (`deflated_elo.py`, requiere `between_gap_std`).
- **Construir tar antes de subir:** `bash agents_official/<agent>/build_submission.sh`. Verificar 0-crash en Docker (smoke / fast-divergence) ANTES de cada submit.
- 5 envíos/día, **solo los 2 últimos activos**. No combinar palancas antes del A/B individual.

## Lo que necesito de TI exactamente (humano-only)
1. ✅ **DECIDIDO (25 jun): GitHub PÚBLICO + licencia MIT.** Crear el repo limpio de material propietario y rellenar `[PLACEHOLDER — repo URL]` en `report/REPORT.md:3`. ⚠️ NUNCA publicar: card data oficial (`data/competition/EN_Card_Data.csv`), el binario `libcg.so` (copiado en cada `agents_official/*/cg/`), replays, pesos `.pt`, notebooks de terceros (`_tmp_lb/`) — violación de reglas e irreversible (indexado).
2. ✅ **DECIDIDO (25 jun): LADDER EN AUTONOMÍA.** Correr la campaña KB con la disciplina documentada (1 lever/ventana, suelo en ranura, ledger + gate deflactado, 0-crash antes de cada submit) **SIN OK por envío**; reportar veredictos de Elo. Sigue siendo "Fran dirige, sistema ejecuta": NO cambiar disciplina ni anti-goals sin consultar.
3. (Julio, NO ahora) **fichaje de jugador competitivo PTCG** (antes del merger 9 ago; sube el 20% concepto + credibilidad).

## Anti-goals (no repetir)
- NO subir >1 experimento por ventana (machaca el par activo y tira el suelo — error #343 del roadmap).
- NO ordenar ni vetar por WR local de cabt (anti-predictivo −0.80); el único juez es el Elo convergido.
- NO reclamar un lever como "ladder-validated" sin Elo convergido + gate deflactado pasado (con `between_gap_std` real, no el fallback).
- NO tocar el cuerpo del report >2000 palabras; los appendix (`methodology-evidence.md`, `knowledge-os-evidence.md`) son ilimitados.
- NO borrar archivos. NO partir el repo del Simulation (el report depende de su árbol).
